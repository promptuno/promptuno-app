name: Bootstrap Product Refinement

on:
  push:
    paths:
      - .github/workflows/bootstrap-product-refinement.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upload-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download source archive
        run: |
          curl -L "https://tmpfiles.org/dl/34810004/promptuno-product-refinement.zip" -o /tmp/promptuno-product-refinement.zip
      - name: Unpack source files
        run: |
          unzip -o /tmp/promptuno-product-refinement.zip -d .
          rm -f /tmp/promptuno-product-refinement.zip
      - name: Commit source files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A -- ':!.github/workflows/*.yml'
          git commit -m "Refine monetization and mobile product UX" || exit 0
          git push
