name: Publish Refined Promptuno

on:
  push:
    paths:
      - .github/workflows/publish-refined-promptuno.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upload-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download refined source
        run: |
          curl -L "https://tmpfiles.org/dl/35682843/promptuno-vibe-fix.zip" -o /tmp/promptuno-vibe-fix.zip
          unzip -q /tmp/promptuno-vibe-fix.zip -d /tmp/promptuno-vibe-fix
      - name: Sync project files
        run: |
          rsync -a --delete \
            --exclude '.git' \
            --exclude '.github' \
            --exclude 'node_modules' \
            --exclude 'dist' \
            /tmp/promptuno-vibe-fix/ ./
      - name: Commit refined app
        run: |
          git config user.name "Promptuno Bot"
          git config user.email "actions@github.com"
          git add -A
          git commit -m "Polish vibe mode and extension behavior" || exit 0
          git push
