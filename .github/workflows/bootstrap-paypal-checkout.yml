name: Bootstrap PayPal Checkout

on:
  push:
    paths:
      - .github/workflows/bootstrap-paypal-checkout.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upload-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download source archive
        run: |
          curl -L "https://tmpfiles.org/dl/34816834/promptuno-paypal-checkout.zip" -o /tmp/promptuno-paypal-checkout.zip
      - name: Unpack source files
        run: |
          unzip -o /tmp/promptuno-paypal-checkout.zip -d .
          rm -f /tmp/promptuno-paypal-checkout.zip
      - name: Commit source files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A -- ':!.github/workflows/*.yml'
          git commit -m "Add PayPal checkout for Pro plan" || exit 0
          git push
