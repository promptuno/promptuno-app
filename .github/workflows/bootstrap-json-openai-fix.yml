name: Bootstrap JSON And OpenAI Fix

on:
  push:
    paths:
      - .github/workflows/bootstrap-json-openai-fix.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upload-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download source archive
        run: |
          curl -L "https://tmpfiles.org/dl/34708823/promptuno-json-openai-fix.zip" -o /tmp/promptuno-json-openai-fix.zip
      - name: Unpack source files
        run: |
          unzip -o /tmp/promptuno-json-openai-fix.zip -d .
          rm -f /tmp/promptuno-json-openai-fix.zip
      - name: Commit source files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A -- ':!.github/workflows/*.yml'
          git commit -m "Fix AI response parsing and backend config" || exit 0
          git push
